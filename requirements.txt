# Phase 0 — ingestion pipeline.
nfl_data_py==0.3.3
pandas==3.0.3
numpy==2.4.6
chromadb==1.5.9
openai==2.44.0
python-dotenv==1.2.2

# nfl_data_py's actual runtime deps; installed with --no-deps because its
# declared pandas<2.0/numpy<2.0 pin is stale (predates pandas 2.0) and
# conflicts with the versions above, which it runs fine against.
appdirs==1.4.4
fastparquet==2026.5.0

# Phase 1 — LangGraph skeleton (router/retrieval/generation/reflection) +
# Streamlit UI, per ADR-006's init_chat_model (no vendor SDK hard-coupling).
# All three provider packages are installed so swapping CHAT_MODEL_PROVIDER
# in .env is a config change, not a pip install.
langgraph==1.2.6
langchain==1.3.11
langchain-anthropic==1.4.7
langchain-openai==1.3.3
langchain-groq==1.1.3
streamlit==1.58.0

# Observability (graph/observability.py, ADR-008) — OTel traces/metrics/logs
# shipped to the collector in observability/docker-compose.yml; LangChain
# auto-instrumentation gives per-LLM-call spans with no graph/llm.py changes.
opentelemetry-api==1.43.0
opentelemetry-sdk==1.43.0
opentelemetry-exporter-otlp-proto-http==1.43.0
opentelemetry-instrumentation-logging==0.64b0
openinference-instrumentation-langchain==0.1.66
